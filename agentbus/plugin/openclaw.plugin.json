{
  "id": "squirrelbus-receiver",
  "name": "AgentBus Receiver",
  "description": "Receives AgentBus push messages from the hub agent via HTTP and injects them as system events into the agent session.",
  "version": "1.0.0",
  "configSchema": {
    "jsonSchema": {
      "type": "object",
      "properties": {
        "token": {
          "type": "string",
          "description": "Bearer token for authenticating incoming push requests. Set via AGENTBUS_TOKEN env var."
        }
      },
      "additionalProperties": false
    }
  }
}
